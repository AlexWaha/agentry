# Memory lives in one SQLite store, .agentry/memory/memory.db, ignored at the
# repository root together with everything else under memory/ except README.md.
# Per-agent markdown memory (agent-memory/) was migrated into the store and
# deleted - nothing here retains it.

# Personal local settings (already gitignored at repo level; listed for portability)
settings.local.json

# Codegraph index lives at the PROJECT root (.codegraph/) - gitignore it there.

# Python bytecode from hook/tool runs
__pycache__/
