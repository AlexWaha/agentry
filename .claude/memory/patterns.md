# L3 Patterns - Reusable Code Patterns Index

> A pattern is code shape used MORE THAN ONCE in this project. Record it here
> so the next task reuses instead of reinventing. Cap: 60 lines. Format:
> `- [P-NNN] <name>: use when <situation> -> patterns/P-NNN-<name>.md`
> The detail file holds the template plus at least 2 observed usage paths.

(no patterns recorded yet)
